# .github/workflows/ping.yml
name: Keep App Alive

on:
  schedule:
    - cron: '*/5 * * * *'   # 5분마다 실행

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping App Service
        run: |
          curl -fsS https://wipi-backend-dpe6cxe4b4dya4g9.koreacentral-01.azurewebsites.net/actuator/health || true
